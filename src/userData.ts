
// creating interface for user's data
export interface userData {
  checkbox: string;
  updated: number;
  country: string;
  population: number;
  continent: string;
  flag: string;
  countryInfo: {
    flag: string;
  };
  menu: string;
}
